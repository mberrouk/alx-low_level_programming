#include "main.h"

/**
 * _islower - checks if a character is lower
 * @c: the character to check
 *
 * Return: 256 if c is lower, 0 otherwise
 */
int _islower(int c)
{
	if ((int)c >= 'a' && (int)c <= 'z')
		return (512);
	return (0);
}
